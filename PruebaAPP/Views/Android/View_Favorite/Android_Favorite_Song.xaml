<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PruebaAPP.Views.Android.ViewModels"
             xmlns:fv="clr-namespace:PruebaAPP.Objetos.Models"
             x:DataType="vm:AndroidPropertyViewModel"
             x:Class="PruebaAPP.Views.Android.View_Favorite.Android_Favorite_Song">

    <Grid RowDefinitions="Auto,*">
        
        <!-- Control y Informacion -->
        <HorizontalStackLayout Margin="20,0,10,10" HorizontalOptions="Start">
            <Border HeightRequest="40" Margin="0,0,10,0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="25"/>
                </Border.StrokeShape>
                <Button Text="Reproducir todo" TextColor="{StaticResource foreg_night_2}" Background="{StaticResource backg_night_0}"/>
            </Border>
            <VerticalStackLayout VerticalOptions="Center">
                <Label Text="Mi lista de favoritos" TextColor="{StaticResource foreg_night_2}"/>
                <Label>
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="{Binding Favoritos.Count}" TextColor="{StaticResource foreg_night_1}"/>
                            <Span Text=" Canciones" TextColor="{StaticResource foreg_night_1}"/>
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </VerticalStackLayout>
        </HorizontalStackLayout>
        
        <!-- Lista -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding Favoritos}" SelectionMode="Single" ItemsUpdatingScrollMode="KeepScrollOffset" ItemSizingStrategy="MeasureFirstItem" SelectionChanged="Click_SelectItems">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="fv:Favorite">
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="20,5">

                        <!-- Cover -->
                        <Border Grid.Column="0" Margin="0,0,10,0" StrokeThickness="0" WidthRequest="110" HeightRequest="80" VerticalOptions="Start">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>
                            <Grid>

                                <Image x:Name="MainImage" Aspect="AspectFill">
                                    <Image.Source>
                                        <UriImageSource Uri="{Binding Thumbnails}" CacheValidity="7.00:00:00" CachingEnabled="True" />
                                    </Image.Source>
                                </Image>

                                <!-- Loader mientras carga -->
                                <ActivityIndicator WidthRequest="50" HeightRequest="50" 
                                                   IsRunning="{Binding Source={x:Reference MainImage}, Path=IsLoading}" 
                                                   Color="{StaticResource foreg_night_2}" 
                                                   IsVisible="{Binding Source={x:Reference MainImage}, Path=IsLoading}" />

                            </Grid>
                        </Border>

                        <!-- Informacion -->
                        <VerticalStackLayout Grid.Column="1" Margin="0,0,10,0">
                            <Label Text="{Binding Title}"    TextColor="{StaticResource foreg_night_2}" LineBreakMode="TailTruncation" MaxLines="2" HorizontalTextAlignment="Justify"/>
                            <Label Text="{Binding Author}"   TextColor="{StaticResource foreg_night_1}"/>
                            <Label Text="{Binding Duracion}" TextColor="{StaticResource foreg_night_2}"/>
                        </VerticalStackLayout>

                        <!-- Icono de eliminar -->
                        <VerticalStackLayout Grid.Column="2" VerticalOptions="Start">

                            <Button Grid.Column="2" 
                                    Text="&#xe872;" 
                                    FontSize="24" 
                                    FontFamily="MaterialIconsRegular" 
                                    TextColor="{StaticResource foreg_night_2}" 
                                    BackgroundColor="Transparent"
                                    Clicked="Click_Delete"/>

                            <Button Grid.Column="2" 
                                    Text="&#xe03b;" 
                                    FontSize="24" 
                                    FontFamily="MaterialIconsRegular" 
                                    TextColor="{StaticResource foreg_night_2}" 
                                    BackgroundColor="Transparent"
                                    Clicked="Click_AddToPlaylist"/>
                        </VerticalStackLayout>

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentView>
